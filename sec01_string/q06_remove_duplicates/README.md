# 5. 중복 문자 제거

-> 입력된 문자열에서 중복된 문자를 제거하고 출력하는 프로그램 작성

### 1) 로직 흐름

1. 문자열의 0번 인덱스부터 끝까지(length()) 한 글자씩 순회한다.
2. `indexOf()` 메서드는 **항상 맨 처음 발견된 문자의 인덱스를 반환한다는 점**을 이용한다.

### 2) 핵심 개념

- `indexOf(char c)의 특성
  - 문자열의 앞에서부터 검색하여 해당 문자가 가장 먼저 발견되는 인덱스를 반환한다.
