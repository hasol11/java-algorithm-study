# 3. 최대 매출

-> 연속된 K일 동안의 최대 매출액을 구하는 프로그램

### 1) 로직 흐름

1. 먼저 처음 윈도우를 구해 answer에 넣어둔다.
2. 한칸씩 밀어야 하는데, sum에 arr[i]를 더하고 arr[i-k]를 뺀다. 즉 다음 칸을 더하고, 맨 앞칸을 빼면 윈도우가 움직인 것과 동일하다.

### 2) 핵심 개념

고정된 크기(K)의 창문을 배열 위에서 한 칸씩 미는 것과 같다. 중복되는 구간의 연산을 피하고 '새로 들어오는 값만 더하고, 빠져나가는 값만 빼는' 방식을 통해 연산량을 줄일 수 있다.
